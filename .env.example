# Public domain (if applicable)
DOMAIN=ratemypdf.com

# Required API credentials
OPEN_AI__org=org-
OPEN_AI__key=sk-
SPOT_TOKEN=
TOOLS_TOKEN=

# App runtime flags
IN_DOCKER=TRUE
RATEMYPDF_DEBUG=false

# Service endpoints (Fly private network)
# Set these after deploying the Redis and Gotenberg apps on Fly
# Use the ".internal" DNS names so requests stay on Fly's private network
REDIS_URL=redis://ratemypdf-redis.internal:6379/0
GOTENBERG_URL=http://ratemypdf-gotenberg.internal:3000
